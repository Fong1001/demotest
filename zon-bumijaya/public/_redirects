# Cloudflare Pages _redirects
# Handles root redirect to default locale since middleware can't run in static export mode

# Root redirect — send / to /en/
/                   /en/                 302
